[project]
name = "payflow"
version = "0.1.0"
description = "Payment orchestration service — Stripe test-mode, idempotent APIs, double-entry ledger, reconciliation"
readme = "README.md"
authors = [
    { name = "Mayank Jain", email = "mayankjainllrl@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.141.1",
    "pydantic>=2.13.5",
    "pydantic-settings>=2.15.0",
    "uvicorn[standard]>=0.52.4",
]

[tool.uv]
package = false
