[project]
name = "payflow"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "mayank", email = "mayank@exitfund.com" }
]
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.141.1",
    "pydantic>=2.13.5",
    "pydantic-settings>=2.15.0",
    "uvicorn[standard]>=0.52.4",
]

[project.scripts]
payflow = "payflow:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
